import 'package:flutter/material.dart';

class DriveView extends StatelessWidget {
  const DriveView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Center(
        child: Text("DriveView"),
      ),
    );
  }
}
